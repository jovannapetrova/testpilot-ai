services:
  - type: web
    name: testpilot-ai-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: python -m pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: DATABASE_URL
        fromDatabase:
          name: testpilot-ai-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: LOG_LEVEL
        value: INFO
      - key: ENVIRONMENT
        value: production
      - key: CORS_ORIGINS
        sync: false
      - key: CORS_ORIGIN_REGEX
        value: "https://.*\\.vercel\\.app"
      - key: ENABLE_TEST_EXECUTION
        value: "false"
      - key: FRONTEND_BASE_URL
        sync: false
      - key: PASSWORD_RESET_TOKEN_MINUTES
        value: "30"
      - key: MAGIC_LINK_TOKEN_MINUTES
        value: "15"
      - key: AUTH_DEBUG_TOKENS
        value: "false"
      - key: MAX_UPLOAD_SIZE_MB
        value: "50"
      - key: MAX_EXTRACTED_SIZE_MB
        value: "150"
      - key: MAX_ARCHIVE_FILES
        value: "5000"

databases:
  - name: testpilot-ai-db
    plan: free
